{% extends 'base.html.twig' %}
{% block title %} Kreiranje automata za igranje {% endblock %}
{% block body %}
<div class="container">
<a href="{{ absolute_url(asset('')) }}game/machines" class="btn btn-primary">Nazad</a>
{{ form_start(form) }}&nbsp;
{{ form_widget(form) }}&nbsp;
{{ form_end(form) }}&nbsp;
</div>
{% endblock %}
